using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gunnery : BaseSystems
{
    [SerializeField] float damageToEnemy = 10;
    [SerializeField] float turretRotationPerSec = 25;
    [SerializeField] float CurrentTurretRotation;
    [SerializeField] float FiringRatePerSec = 5;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
